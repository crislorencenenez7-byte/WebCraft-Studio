export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({ok:false,error:"Method not allowed"});const apiKey=process.env.RESEND_API_KEY,adminEmail=process.env.ADMIN_EMAIL;if(!apiKey)return res.status(500).json({ok:false,error:"RESEND_API_KEY is not configured in Vercel."});if(!adminEmail)return res.status(500).json({ok:false,error:"ADMIN_EMAIL is not configured in Vercel."});try{const b=req.body||{},clientName=b.clientName||"Unknown Client",clientEmail=b.clientEmail||"No email provided",projectName=b.projectName||"Untitled Project",packageName=b.package||"Unknown Package",price=Number(b.price||0),requirements=b.requirements||"No requirements provided.",projectId=b.projectId||"No project ID",attachments=Array.isArray(b.attachments)?b.attachments:[];if(attachments.length>5)return res.status(400).json({ok:false,error:"Maximum of 5 design attachments allowed."});const html=`<div style="font-family:Arial,sans-serif;line-height:1.6;color:#222"><h2>New WebCraft Studio Project</h2><p>A client has submitted a new website project.</p><hr><p><strong>Project Name:</strong> ${escapeHtml(projectName)}</p><p><strong>Client Name:</strong> ${escapeHtml(clientName)}</p><p><strong>Client Email:</strong> ${escapeHtml(clientEmail)}</p><p><strong>Package:</strong> ${escapeHtml(packageName)}</p><p><strong>Price:</strong> ₱${price.toLocaleString("en-PH")}</p><p><strong>Project ID:</strong> ${escapeHtml(projectId)}</p><hr><h3>Requirements</h3><p>${escapeHtml(requirements).replace(/\n/g,"<br>")}</p><p><strong>Client Design:</strong> ${attachments.length} attachment(s) included.</p><p>Open the Admin Editor to review the project, client designs and payment transaction.</p><p><a href="${getEditorUrl(projectId)}">Open Admin Editor →</a></p></div>`;const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({from:process.env.RESEND_FROM_EMAIL||"WebCraft Studio <onboarding@resend.dev>",to:[adminEmail],reply_to:clientEmail,subject:`New Project — ${projectName} — WebCraft Studio`,html,attachments:attachments.map(a=>({filename:a.filename,content:a.content}))})});const data=await response.json();if(!response.ok)return res.status(response.status).json({ok:false,error:data});return res.status(200).json({ok:true,message:"Project email sent successfully.",id:data.id})}catch(e){console.error(e);return res.status(500).json({ok:false,error:e.message||"Unable to send project email."})}}
function escapeHtml(v){return String(v).replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;","\"":"&quot;"}[c]))}function getEditorUrl(id){const base=process.env.SITE_URL||"https://web-craft-studio-swart.vercel.app";return `${base}/admin/editor.html?id=${encodeURIComponent(id)}`}
